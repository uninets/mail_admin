package MailAdmin::Aliases;
use Mojo::Base 'Mojolicious::Controller';

sub add {
    my $self = shift;

    $self->render();
}

sub update_or_create {
    my $self = shift;

    $self->render();
}

sub read {
    my $self = shift;

    $self->render();
}

sub delete {
    my $self = shift;

    $self->render();
}


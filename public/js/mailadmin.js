// Generated by CoffeeScript 1.3.3
(function() {
  var flash;

  flash = function(message, cl) {
    var flash_msg;
    flash_msg = humane.create;
    return humane.log(message, {
      timeout: 5000,
      clickToClose: true,
      addnCls: cl
    });
  };

  $(document).ready(function() {
    var cls, text;
    $('html').removeClass('no_js');
    $('.fancy-link').fancybox({
      fitToView: true,
      autoSize: false,
      width: '710px',
      height: 'auto',
      closeClick: false,
      showCloseButton: false,
      transitionIn: 'fade',
      transitionOut: 'fade'
    });
    $('.delete').click(function() {
      var answer;
      return answer = confirm("Are you sure you want to delete the selected object?");
    });
    if ($('#flash-msg').length > 0) {
      text = $('#flash-msg').text();
      cls = $('#flash-msg').attr('class');
      $('#flash-msg').remove();
      return flash(text, cls);
    }
  });

}).call(this);
